# 项目：AI 智能数据分析与可视化中台 (AI Data Analysis Platform)

## 🎯 项目简介
基于大语言模型（LLM）驱动的智能化数据分析平台，旨在将传统的 BI（商业智能）和数据查询流程彻底平民化。系统支持多数据源接入与灵活管理，核心通过 **智能 Agent (ReAct 架构)** 将用户自然语言意图动态转化为安全的 SQL 执行流，并自动推导渲染数据图表，从而提供无缝的「问答即洞察」体验。

## 💡 核心亮点与挑战突破

### 1. 基于 Agent 的动态决策引擎 (ReAct Architecture)
- **挑战**：固定链路的“文生SQL”无法满足真实场景对复杂查询的校验和可视化需求。
- **方案**：搭建自主智能数据分析 Agent 环，工作流拆解为：`意图解析 -> 防腐SQL生成 -> 自动纠错防腐层执行 -> 数据返回 -> 可视化与图表特征提取 -> 最终研判与文字总结`。在架构上严格划定 **自主决策大脑 (Agent)** 与 **被动执行器 (Tool)** 的边界。
- **价值**：避免了单一 Prompt 带来的脆弱性，各步骤高度解耦，分析准确度与系统扩展性大幅提升。

### 2. 动态 SQL 防腐层与自愈机制 (Self-Healing SQL Executor)
- **挑战**：LLM 常常不可控，直接将生成的 SQL 扔进数据库不仅极易报错，还可能存在越权查表等安全风险。
- **方案**：
  - **基于 AST 的安全扫描**：拦截并解析 LLM 生成的 SQL，通过 Node SQL Parser 进行词法树（AST）强校验，仅放行纯 `SELECT`/`SHOW` 语句，并动态强制注入 `LIMIT` 安全阈值拦截大规模扫表。
  - **“只修语法不猜语义”的静默自愈机制**：当 SQL 执行因纯语法维度（如 GROUP BY 限制、保留字冲突等）报错时，通过异常拦截并在沙箱内触发辅助大模型二次重写；若遭遇查无此列等业务级异常，则拒绝掩盖并抛出，彻底根除 LLM 发散联想带来的脏数据危险。

### 3. 表级 Schema AI 自动升维与动态增强 (Schema Enrichment)
- **挑战**：真实的业务数据库常常没有规范的字段注释，当遇到含糊的字段名和类型值（如 `status: 1/2/3`）时，LLM “文生SQL” 的准确率一塌糊涂。
- **方案**：
  - 增设 **自动注释增强 (Schema Enrichment)** 机制。
  - 在用户导入数据源大文件建表完毕后，利用非阻塞异步任务进行数据“探针诊断”：自动 `SELECT * LIMIT 5` 提取少许真实数据样本。
  - 将原生结构和抽样数据推送至 LLM，推导出字段真实的业务枚举含义（如: “状态字段，取值1为激活，0为封禁”）、货币单位、日期格式，并自动反写回 MySQL `COLUMN_COMMENT` 中。
  - **价值**：底层结构注入了极其丰富的领域逻辑（Domain-knowledge），彻底消灭由于大模型对模糊字段幻觉导致的分析失败，查询精确度得到了根本性飞跃。

### 4. 实时交互反馈矩阵 (SSE 与多路流式处理)
- **挑战**：Agent 深思熟虑和多步工具调用时间通常需数秒至数十秒，传统的等待 Loading 导致极差的用户体验。
- **方案**：
  - 弃用传统长轮询或 WebSocket，采用 **Server-Sent Events (SSE)** 流式输出底座构建异步交互通道。
  - 配合前端定义的高内聚 `MessageBlock` 数据结构，将后端工具栈中的每一步微小动作（从思考路径、执行 SQL 执行态、取数结果到 ECharts 配置下发）实现毫秒级 UI 实时呈现。
  - **价值**：显著削弱用户的等待焦躁感，在交互上达到专业分析师一步步推演的“仪式感”。

### 5. 工业级多级分层记忆引擎 (Multi-Tier Memory Engine)
- **挑战**：BI 场景下 SQL 结果集动辄数千 Token，简单的滑窗截断会导致“前聊后忘”，丢弃核心业务口径。
- **方案**：
  - **Tier 1 (Working Memory) - 基于距离衰减的三级上下文计算**：自研 PromptBuilder，在 Token 预算限制下采用 `full -> medium -> compact` 三级脱水计算。将远距历史自动坍缩为分析结论，中距历史剥离数据保留核心 SQL 拓扑，近距历史保留完整思考链，在有限边界下最大化锁定历史语义熵。
  - **Tier 2 (Semantic Memory) - 异步知识蒸馏**：引入后台异步“蒸馏”机制，每轮对话后自动增量更新会话的 **State JSON (逻辑状态库)**。
  - **动态 Prompt 注入**：将 Tier 2 的语义事实钉在 System Prompt 顶部，确保即使原始消息因 Token 限制被截断，Agent 也能长效保留“高价值用户定义”等业务共识。
- **价值**：极大平衡了逻辑连贯性与 Token 开销成本。

### 6. 极速批处理数据入库系统
- **高并发数据上传**：自研内置内存流 `Readable Stream` 模拟管道技术，绕过磁盘 IO 实现极速 `LOAD DATA LOCAL INFILE` 指令下发，提速传统 ORM 批量 `INSERT` 数十倍，支撑千万级行数的高阶分析。

## 📊 技术栈
- **后端引擎**：NestJS, TypeScript
- **前端驱动**：Vue 3 + Composition API, Pinia, Tailwind CSS, NuxtUI
- **大模型基建**：OpenAI SDK, LangSmith 集成
- **储存/持久化**：MySQL 8 (TypeORM / 原生 Driver)
- **核心工具**：Node SQL Parser (AST校验), SSE 流化响应引擎, Apache ECharts
